import 'package:flutter/material.dart';

class BroadCastingScreen extends StatelessWidget {
  static String routeName = '/broadcast';
  const BroadCastingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
